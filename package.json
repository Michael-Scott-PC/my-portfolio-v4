{
  "scripts": {
    "client": "cd portfolio-frontend && yarn && yarn run build && yarn run serve",
    "server": "cd portfolio-backend && yarn && yarn run build && yarn run start",
    "start": "concurrently \"yarn run server\" \"yarn run client\""
  },
  "dependencies": {
    "concurrently": "^5.1.0"
  }
}
